{
  "name": "ci-cd-sederhana",
  "version": "1.0.0",
  "main": "src/sum.js",
  "scripts": {
    "test": "npx jest",
    "build": "rm -rf dist && mkdir dist && cp -r public/* dist/"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
